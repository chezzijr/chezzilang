# rule: expr, orExpr, andExpr, eqExpr, relExpr, addExpr, mulExpr, unary, coalesceExpr, notExpr
fn main():
    a := 1 + 2 * 3
    b := x and y or z
    c := not done
    d := -n + 1
    e := p < q == r
    f := a ?? 0 + 1
    g := not x in xs
    h := -n ?? 2 * 3
