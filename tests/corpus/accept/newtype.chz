# rule: newtypeDecl
newtype UserId = int
newtype Meters = float:
    fn double(self) -> Meters:
        return self + self
newtype Stack[T] = List[T]
newtype Boxed[T] = T:
    fn same(self) -> Boxed[T]:
        return self
uid := UserId(10)
print(int(uid))
