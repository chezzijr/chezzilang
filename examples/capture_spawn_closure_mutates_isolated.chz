# Uniform by-reference capture (F3 — the mutation form, parity guard for the closure airlock arm).
# A closure `f` captures a by-reference local `xs` (a cell holding a list) and MUTATES its inner heap
# value through a method call (`xs.push(2)`) — the cell is not rebound, but its contents change. When
# `f` crosses a `spawn` task boundary as an argument to `spawn invoke(f)`, the task receives an ISOLATED
# deep copy of the cell (and its list) — TICKET-169 (D4 layer C) makes the task's write to that copy
# a runtime FAULT (`recover:` catches it) rather than a silent push into a copy the parent never
# sees. Direct `spawn f()` is now a compile-time error. Output is `[1]` here because spawn arguments
# remain a declined shape and layer C catches the write.
# DO NOT "fix" toward sharing — the isolation IS the memory-safety line (design §4
# F1/F3): a plain capture into a task is snapshot-copied; use `Shared[T]` to share across tasks.
fn invoke(g: fn() -> nil):
    _ := recover: g()

fn main():
    xs := [1]
    f := fn(): xs.push(2)
    parallel:
        spawn invoke(f)
    print(xs)                   # [1] (task's write to its own isolated copy faulted)

main()
